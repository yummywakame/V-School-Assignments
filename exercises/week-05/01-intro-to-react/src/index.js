import React from 'react'
import ReactDOM from 'react-dom'
import App from './App.js'

// Components - A function that returns JSX
// You can only return a single element, so if multi, enclose in a div




// arg1: what do you want me to render
// arg2: where do you want me to render
// it looks like html but its really JSX
ReactDOM.render(<App />, document.getElementById('root')) 