import React from 'react'
import ReactDOM from 'react-dom'
import App from './App.js'

// Props is an object that is filled with the properties we give
// the component

// The compnent receives properties as an object which it uses
// to dispay its data
// There are no reserved words for Props

ReactDOM.render(<App />, document.getElementById('root')) 