import React from 'react'

// Hides the contents until the stylesheet has loaded
// Sets background colour to theme saved in localStorage
const Loader = () => {
    return (
        <div id="loader">Loading Spinner</div>
    )
}

export default Loader